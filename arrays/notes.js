const notes = [{
    title: 'My next'
}];

//console.log(notes.length);
//notes[0] = 'Notes 4';
//console.log(notes[0]);

//adds an element at the end of an array
//notes.push('ABC');

//removes an element from the end of the array
//notes.pop();

//add an element at the front of an array
//notes.unshift('PQR');

//removes an element from the front of an array
//notes.shift();

//starts from the specified index and removes the specified number of elements
//notes.splice(1, 1);
//console.log(notes);

//notes.forEach(function() {
//    console.log('Hello');
//})

// takes the element as an input and returns its index
console.log(notes.indexOf('Notes 2'));