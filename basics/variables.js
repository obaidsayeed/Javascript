//you cannot define a variable twice
let petName = 'alex';
petName = 'biscuit';

console.log(petName);

//There are rules to name a variable
let last_ = 2;
let result = 3 + 4;

//Variable names can't be reserved keywords
let
let = 2;