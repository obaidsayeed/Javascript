// === - equality operator
let temp = 32;
let isFreezing = temp === 32;

console.log(isFreezing);

// Create an age variable and set the value of age variable to your age

let age = 7;

if (age <= 7) {
    console.log('Child pricing');
}

if (age >= 65) {
    console.log('Senior Discount');
}